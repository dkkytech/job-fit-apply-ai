# OS
.DS_Store
Thumbs.db

# IDE
/.idea
.idea/
.vscode/
*.swp
*.swo
*~

# Environment files (defense-in-depth — subprojects have their own rules too)
.env
.env.*
!.env.example
!.env.ci.example
# Committed template for a second (test) instance — copy to gitignored .env.test.
!.env.test.example

# Logs
*.log
logs/

# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/

# Coverage
coverage/

# Test artifacts
allure-results/
test-results/

# Lockfiles (pick one per project)
bun.lockb
/services/.gradle

# Kotlin/Eclipse compiled output (e.g. services/*/bin/main, bin/test)
**/bin/
**/.gradle/

# E2E slice per-run state (docker-compose.e2e.yml bind-mount sources)
/.e2e/
# Second ("source") e2e slice for the multi-instance scenarios (#56 sc. 9/10)
/.e2e-src/
